# DESIGN.md · 设计规范

> 适用：gh-release-center（软件下载中心）
> 主题：深色模式 · 主色 #ff9292 · 磨砂玻璃
> 基线：2026-08-06（v0.1.5）

---

## 一、色彩系统

### 1.1 主色阶（#ff9292 系，5 档）

| Token | 色值 | 用途 |
|---|---|---|
| `--pink` | `#ff9292` | 主色：主按钮 / 选中态 / 强调文字 / 下载按钮 |
| `--pink-h` | `#ff7a7a` | hover 态（比主色深一档，保持同色相） |
| `--pink-a` | `#e67878` | active / 按压态 |
| `--pink-b` | `#cc7575` | 主色边框（弱化描边） |
| `--pink-d` | `#995858` | 深边框 / 分隔 |
| `--pink2` | `#ffb3b3` | 深底上的亮粉（弱强调文字，仅深色底使用） |

**规则**：所有粉色 hover/active 一律走色阶（`#ff7a7a`/`#e67878`），**禁止**用 `brightness()`/`opacity` 变体替代——色阶是同色相饱和度递进，滤镜变体会偏灰偏脏。

### 1.2 辅助色

| Token | 色值 | 用途 |
|---|---|---|
| `--cyan` | `#92ffff` | **互补青**：链接 / 信息提示（与主粉冷暖平衡） |
| `--peach` | `#ffc892` | **蜜桃**：警告 / 暖标签 |
| `--ppink` | `#ff92c8` | **粉紫**：分类 / 次级强调 |
| `--bpink` | `#b0a0ff` | **蓝紫**：次级强调 |
| `--purple` | `#a78bfa` | 紫（保留：分类 tile / 历史兼容） |

**主辅搭配原则**：主粉 + 互补青是"品牌对"，一个场景里同时出现时，粉承担"行动/主操作"，青承担"信息/导航"。粉紫/蓝紫只做分类色或点缀，**不做大面填充**。

### 1.3 语义色

| Token | 色值 | 用途 |
|---|---|---|
| `--green` | `#6dd58c` | 成功 / LATEST 徽标 / 版本徽标 |
| `--red` | `#ff6b6b` | 危险 / 删除 / 错误提示 |
| `--amber` | `#ffc892` | 警告（与蜜桃同值，语义别名） |
| `--coral` | `#ff8a5c` | NEW 角标（更新提醒） |

### 1.4 中性色（深色基底）

| Token | 色值 | 用途 |
|---|---|---|
| `--bg` | `#141419` | 页面基底 |
| `--panel` | `#1d1d26` | 面板（降级背景） |
| `--panel2` | `#24242f` | 次级面板 / 输入框 |
| `--line` | `#2e2e3d` | 分隔线 / 边框 |
| `--fg` | `#e8e8f0` | 正文 |
| `--dim` | `#9a9ab0` | 次级文字 |
| `--faint` | `#6c6c85` | 弱提示文字 |

---

### 1.5 文字对比度规范（★ 最易错，务必遵守）

核心事实：**#ff9292 上不能放白字**。

| 组合 | 对比度 | 判定 |
|---|---|---|
| 白字 on `#ff9292` | 2.15:1 | ❌ FAIL（不可读） |
| **深色字 `#2d1414` on `#ff9292`** | **9.78:1** | ✅ AAA |
| `#ff9292` 文字 on 深底 `#141419` | 9.78:1 | ✅ AAA（深底上可做粉色文字） |
| 白字 on `#ff8a5c`（NEW 角标） | ~2.3:1 | ❌ FAIL |
| 深色字 on `#6dd58c`（LATEST） | 高对比 | ✅ 深字 `#0d2416` |

**落地规则**：

1. **亮色块上 → 深色文字**：凡底色是 `#ff9292` 系、`#ff8a5c`、`#6dd58c` 等亮色，文字一律用深色 `#2d1414`（绿色徽标上可用 `#0d2416`），**禁止白字**。
2. **深色块上 → 浅色文字**：深底（bg/panel）上用 `#e8e8f0` 正文、`#9a9ab0` 次级；深底上的粉色文字（如下载按钮描边粉字）用 `#ff9292`，对比度 9.78:1 达标。
3. **小字号优先保对比**：10-12px 的字（角标、徽标）必须满足 4.5:1 以上，宁可加深文字色也不放大字号凑。
4. 不确定时套用规则 1：**底亮 → 字深（#2d1414），底深 → 字浅（#e8e8f0）**。

### 1.6 CSS Token 定义（可直接使用）

```css
:root{
  --bg:#141419; --panel:#1d1d26; --panel2:#24242f; --line:#2e2e3d;
  --fg:#e8e8f0; --dim:#9a9ab0; --faint:#6c6c85;
  --pink:#ff9292; --pink-h:#ff7a7a; --pink-a:#e67878; --pink-b:#cc7575; --pink-d:#995858;
  --pink2:#ffb3b3;
  --on-pink:#2d1414;
  --cyan:#92ffff; --peach:#ffc892; --ppink:#ff92c8; --bpink:#b0a0ff; --purple:#a78bfa;
  --green:#6dd58c; --coral:#ff8a5c; --amber:#ffc892; --red:#ff6b6b;
}
```

---

## 二、磨砂玻璃规范

### 2.1 原理

磨砂玻璃 = **半透明底色 + backdrop-filter 模糊 + 浅色描边 + 顶部高光**，让元素"浮"在背景光影之上。玻璃感依赖背景有内容可磨，所以页面必须配**背景光斑层**。

### 2.2 背景光斑层（玻璃的前提）

```css
body::before{
  content:"";position:fixed;inset:0;z-index:-1;pointer-events:none;
  background:
    radial-gradient(640px 420px at 12% -6%,  rgba(255,146,146,.16), transparent 70%),
    radial-gradient(760px 520px at 90% 110%, rgba(146,255,255,.11), transparent 70%),
    radial-gradient(540px 400px at 62% 128%, rgba(167,139,250,.10), transparent 70%);
}
```

- 三色 = 主粉 / 互补青 / 紫，透明度 0.10-0.16（宁弱勿强，强了抢内容）
- `position:fixed` 让光斑固定，滚动时玻璃与光斑产生层次错位，玻璃感更强
- `pointer-events:none` 不挡交互

### 2.3 组件玻璃参数（三档层级）

| 层级 | 元素 | background | backdrop-filter | border | box-shadow |
|---|---|---|---|---|---|
| 轻玻璃 | 卡片 | `rgba(255,255,255,.045)` | `blur(14px) saturate(140%)` | `1px rgba(255,255,255,.09)` | `inset 0 1px 0 rgba(255,255,255,.08)` |
| 中玻璃 | 顶栏 | `rgba(20,20,25,.55)` | `blur(16px) saturate(140%)` | 下边 `1px rgba(255,255,255,.08)` | `inset 0 1px 0 rgba(255,255,255,.06)` |
| 重玻璃 | 弹层 | `rgba(29,29,38,.82)` | `blur(20px) saturate(140%)` | `1px rgba(255,255,255,.14)` | `inset 0 1px 0 rgba(255,255,255,.12), 0 18px 50px rgba(0,0,0,.5)` |
| 遮罩 | 弹层背板 | `rgba(8,8,12,.5)` | `blur(4px)` | — | — |

**参数逻辑**：越靠近用户/越重要的层，不透明度越高、blur 越大（弹层最实最糊，卡片最透最清）。卡片 hover：`transform:translateY(-2px)` + 描边换粉 `rgba(255,146,146,.75)`。

### 2.4 使用规则

1. **三要素缺一不可**：半透明底色 + blur/saturate + 白描边（1px）；顶部高光（inset）是"玻璃厚度"的来源，薄玻璃可以没有，但至少要有描边。
2. **嵌套最多一层**：玻璃卡片内不再放玻璃容器（避免 blur 叠加拖性能、视觉糊成一团）；卡片内的输入框/按钮用**纯半透明实底**（`rgba(36,36,47,.75)` 这类）。
3. **hover 不加 blur**：hover 只做位移 + 描边变化，不增大 blur 值（每帧重算模糊成本高）。
4. **saturate(140%) 必带**：让玻璃下的光斑色彩更饱和，这是"通透感"的关键，只用 blur 会发灰。
5. **白描边透明度参考**：0.08-0.14，随层级加深；超过 0.2 会变"磨砂塑料"，失去通透感。

### 2.5 降级策略（无 backdrop-filter 环境）

```css
@supports not ((backdrop-filter:blur(1px)) or (-webkit-backdrop-filter:blur(1px))){
  header{background:rgba(20,20,25,.94)}
  .card{background:rgba(255,255,255,.05)}
  .modal{background:#1d1d26}
}
```

降级后 = 纯半透明/实底面板，光斑层依然可见，视觉仍成立——**玻璃是增强不是必需**。

### 2.6 性能注意

- blur 由 GPU 合成，卡片量 16-30 张无压力；避免在滚动容器内嵌套多层 blur
- 光斑层 fixed 不随内容重绘；blur 半径固定值（14/16/20px），不做动画
- 若设备卡顿：优先降卡片 blur 到 8px 或去掉 saturate，而非去掉光斑

---

## 三、检查清单（改动后自查）

- [ ] 亮色底（#ff9292/#ff8a5c/#6dd58c）上的文字是深色（#2d1414/#0d2416），无白字
- [ ] 新增颜色来自 token，未硬编码新 hex
- [ ] hover/active 用的是色阶（--pink-h/--pink-a），非滤镜变体
- [ ] 玻璃组件三要素齐全（底色/blur/描边），嵌套 ≤1 层
- [ ] 新玻璃组件已加 @supports 降级
